.house {
  form {
    padding-bottom: 1em;
  }

  .__name {
    margin-bottom: 0;
    margin-top: -1em;
  }

  .info {
    background: darken(light-color, 20%);
    // border-bottom: solid 1px darken(light-color, 20%);
    // color: action-color;
    margin-bottom: 1em;
    padding: 0.4em 0.8em;

    p {
      margin-left: 0;
      margin-right: 0;
    }

    .value {
      color: action-color;
    }
  }
}
