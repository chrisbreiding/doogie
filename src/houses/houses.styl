.list-house {
  background: light-color;
  color: action-color;
  display: flex;

  i {
    @extends .center-contents;
    cursor: move;
    cursor: grab;
    cursor: -moz-grab;
    cursor: -webkit-grab;
    width: 2.8em;
  }

  a {
    flex-grow: 1;
  }
}
