.list-house {
  p {
    color: lighten(dark-color, 20%);
    font-size: 0.9em;
    margin: 0.2em 0 0;
  }
}

.houses {
  header {
    margin-bottom: 0;
  }

  .container {
    overflow-x: scroll;
    width: 100%;
  }

  main {
    padding: 0.5em;

    .house {
      border: solid 0.1em darken(light-color, 40%);
      bottom: auto;
      float: left;
      left: auto;
      margin: 0.5em;
      position: static;
      right: auto;
      top: auto;
      width: 20em;
    }

    header {
      display: none;
    }

    .__name {
      margin-top: 0;
    }
  }
}
