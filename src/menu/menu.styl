.menu {
  > li {
    margin: 1em 0;
  }

  li li {
    border-top: solid 1px darken(light-color, 10%);

    &:first-child {
      border-top: none;
    }
  }

  @media (max-width: 30em) {
    .houses-link {
      display: none;
    }
  }
}
